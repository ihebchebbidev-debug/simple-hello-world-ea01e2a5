// Driver app uses vector icons only (MaterialCommunityIcons via @expo/vector-icons).
// PNG requires from an external project cause Metro resolver issues.
// All icon names map to VECTOR_FALLBACK in src/components/Icon.js.
export const Icons = {};
